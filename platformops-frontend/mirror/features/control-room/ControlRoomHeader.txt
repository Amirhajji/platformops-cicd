ORIGINAL FILE: C:\Users\h50048240\3D Objects\PlatformOPS\platformops-frontend\src\features\control-room\ControlRoomHeader.tsx
==================================================
// src/features/control-room/ControlRoomHeader.tsx
import { Play, RotateCcw, Siren } from "lucide-react";
import { useEvaluateActions } from "../../hooks/useEvaluateActions";

export function ControlRoomHeader() {
  const { evalAlerts, resetEval, evalIncidents, isBusy } =
    useEvaluateActions();

  const status =
    evalAlerts.isPending
      ? "Evaluating alertsâ€¦"
      : resetEval.isPending
      ? "Resetting & evaluatingâ€¦"
      : evalIncidents.isPending
      ? "Evaluating incidentsâ€¦"
      : null;

  return (
    <div className="space-y-2">
      <div className="flex items-center justify-between">
        <h1 className="text-xl font-semibold">Control Room</h1>

        <div className="flex gap-2">
          <button
            disabled={isBusy}
            onClick={() => evalAlerts.mutate()}
            className="inline-flex items-center gap-2 rounded bg-white px-3 py-2 text-sm font-semibold text-zinc-950 disabled:opacity-50"
          >
            <Play size={14} />
            Evaluate Alerts
          </button>

          <button
            disabled={isBusy}
            onClick={() => resetEval.mutate()}
            className="inline-flex items-center gap-2 rounded border border-zinc-700 bg-zinc-900 px-3 py-2 text-sm disabled:opacity-50"
          >
            <RotateCcw size={14} />
            Reset & Evaluate
          </button>

          <button
            disabled={isBusy}
            onClick={() => evalIncidents.mutate()}
            className="inline-flex items-center gap-2 rounded border border-red-900 bg-red-950/40 px-3 py-2 text-sm text-red-300 disabled:opacity-50"
          >
            <Siren size={14} />
            Evaluate Incidents
          </button>
        </div>
      </div>

      {status && (
        <div className="text-xs text-zinc-400">
          â³ {status}
        </div>
      )}
    </div>
  );
}

